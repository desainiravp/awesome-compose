version: '3.7'
services:
  frontend:
    image: react-nginx-image-new:latest
    ports:
      - "80"
    deploy:  
      replicas: 1
      placement:
       constraints:
          - node.role == worker     
      resources:
        limits:
          cpus: "0.2"
          memory: 100M
      
        
