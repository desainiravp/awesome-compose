version: '3'
services:
  frontend:
    image: react-nginx-image-new:latest
    ports:
      - "80"
    deploy:
      replicas: 2
      placement:
        constraints:          
          - node.resources.cpu >= 2
          - node.resources.memory >= 4G
      
        
